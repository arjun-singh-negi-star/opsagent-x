# Copy this file to 01-secrets.yaml, fill in real values, and DO NOT commit
# 01-secrets.yaml. Add it to .gitignore, or better, manage it with
# sealed-secrets / external-secrets / your cloud provider's secret manager.
apiVersion: v1
kind: Secret
metadata:
  name: opsagent-x-secrets
  namespace: opsagent-x
type: Opaque
stringData:
  NVIDIA_API_KEY: "nvapi-replace-me"
  GITHUB_TOKEN: "replace-me"
  ARGOCD_TOKEN: "replace-me"
  MONGO_URI: "mongodb://mongo:27017"
  REDIS_URL: "redis://redis:6379/0"
